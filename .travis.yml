language: php

php:
    - '7.0'
    - '7.1'
    - '7.2'
    - '7.3'
    - '7.4'
    - nightly

matrix:
    fast-finish: true
    allow_failures:
        - php: '7.0'
        - php: '7.1'
        - php: '7.2'
        - php: nightly

install: composer install --ignore-platform-reqs

script: composer test
